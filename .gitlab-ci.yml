default:
  image: node:current-alpine

stages:
  - check

server-format:
  stage: check
  script:
    - cd server
    - npm install -g prettier
    - prettier --check "src/**/*.ts"

server-build:
  stage: check
  script:
    - cd server
    - npm install
    - npm run build
